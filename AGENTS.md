# Agent Guidelines
 
 Keep codebase clean and functional. When creating new routes, use TanStack Start route conventions in `src/routes`.
 
- Follow existing patterns in `src/components/ui/` for UI components.
- Run `npm run build` and `npm run lint` before committing changes.
